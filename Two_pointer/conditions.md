✅ Core Conditions
Array/List is sorted (or can be sorted)
You need to find a pair / triplet / subarray based on a condition (sum, difference, etc.)
Problem involves continuous traversal from both ends or sliding window
You want to optimize brute force (O(n²) → O(n))
